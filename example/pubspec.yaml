name: example
description: A new Flutter project.

publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  adwaita: ">= 0.0.3"
  bitsdojo_window:
  libadwaita:
    path: ../
  window_decorations: ">=0.9.5 <1.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
