name: example
description: A new Flutter project.

publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  adwaita: "0.0.4"
  bitsdojo_window: ">=0.1.1+1 <1.0.0"
  libadwaita:
    path: ../
  multi_window: ">=0.2.0 <1.0.0"
  window_decorations: ">=1.0.0 <2.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^2.4.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png