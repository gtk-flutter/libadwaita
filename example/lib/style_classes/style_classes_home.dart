import 'package:flutter/material.dart';

class StyleClassesHomePage extends StatelessWidget {
  const StyleClassesHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(children: []);
  }
}
